# 智能安全帽 Android 开发与验收指南

版本：1.5  
编制日期：2026-08-11  
目标板卡：DS-H618-D3，Android 12，API 31

## 1. 基准与目录

- 工程根目录：`/Users/caxzz/Desktop/android-helmet`
- 旧 Linux 项目：`/Volumes/新加卷/头盔备份`，只读
- 需求基准：`/Users/caxzz/Desktop/android-helmet/最终版安全帽说明书.docx`
- 需求基准 SHA-256：`105cac44d389ead143007f73ff94bb59fd2fc1041e9ffc397ced331a729481cd`
- 开发板 ADB 序列号：`2c001031774186e21d3`

所有新代码、配置、测试和文档只写入工程根目录。旧项目、虚拟机和压缩包只用于分析。

## 2. 工程范围

交付内容：

- Android 12 设备应用。
- 用户态 USB、UART、I²C、GPIO、JNI 和 Android API 外设接入。
- 后台、管理端和开发板联调环境。
- 说明书第 5.1 至 5.6 的软件实现、自动测试、开发板验证和文档。
- 外部模块协议和适配器，但不开发模块内部软件。

不包含：

- Android 系统固件、内核和设备树。
- 外置控制器或传感器模块固件。
- 整机更新、生产签名和量产烧录。
- 硬件结构、EMC、IP 防护和产品认证。

外部摄像头、GNSS/RTK、LoRa、IMU、近电、高度和电池模块通过现有 Android API、USB、UART、I²C、JNI 或厂商接口接入。若现有接口不可用，将其记录为外部依赖。

## 3. 迁移结论

旧程序依赖 i.MX8MM 的 RPMsg、V4L2、ALSA、GStreamer、PulseAudio 和 Linux 设备节点，不能直接重新编译为 APK。

迁移后的四层结构：

1. Android 应用：业务、存储、联网、媒体、定位、算法、本地提示和后台通信。
2. 用户态硬件层：标准 Android API、厂商 SDK、AIDL/JNI 和受限设备节点。
3. 后台：身份、状态、轨迹、媒体、通话、广播、告警、审计和对象存储。
4. 管理端：权限、地图、轨迹、媒体、通话、广播和告警处置。

## 4. 当前板卡能力

| 项目 | 当前结果 |
|---|---|
| Android | Android 12，API 31 |
| ADB | 可连接并具有开发调试权限 |
| 摄像头 | CameraService 未发现设备 |
| 定位 | 无 GNSS fix |
| 传感器 | 未发现可验收的 IMU、近电或高度模块 |
| 电池 | 系统报告无电池 |
| 蜂窝 | 未发现可用 4G/5G 模块 |
| 串口 | 已验证 `/dev/ttyAS2` 用户态打开关闭 |
| I²C/GPIO | 需按评估板资料确认可用节点和接线 |
| 音频 | PCM 播放路径可写，采集返回全零 |
| 网络 | 有线网络存在；其他网络待模块验证 |

本工程使用当前开发固件提供的既有权限，不修改系统软件。

## 5. 原型迁移原则

可复用：

- 业务事件含义和按键语义。
- NMEA 解析思路。
- 设备状态字段和管理端信息结构。

必须重写：

- RPMsg 和 Linux 进程控制。
- V4L2、ALSA、GStreamer 等 Linux 专用媒体路径。
- 持久化、网络生命周期、鉴权和可靠上传。
- Android 前台服务、开机恢复和权限流程。

必须删除固定值：

- 固定设备主题。
- 固定电量和电压。
- 固定经纬度。
- 硬编码服务器、令牌和地图密钥。

## 6. 功能实现要求

### 6.1 实时定位

- 使用系统定位或外部 GNSS 的真实数据。
- 保存 fix、精度、卫星数、速度、方向和时间质量。
- 轨迹先持久化，再可靠上传。
- 电子围栏支持滞回和处置审计。
- RTK 使用真实接收机、天线和差分服务，现场误差不大于 0.5 米。

### 6.2 拍照与录像

- 物理按键触发拍照和录像。
- 使用 Camera2 或 UVC 可见摄像头。
- 录像目标为连续 1080P。
- 媒体绑定设备、人员、时间、位置、告警、大小和 SHA-256。
- 支持离线缓存、断点续传和幂等归档。

### 6.3 语音视频通话

- 物理按键发起语音或视频呼叫。
- 后台接听、拒绝和挂断。
- 使用 WebRTC、STUN 和 TURN 完成实时媒体。
- 文字广播自动播放并返回送达和播放结果。
- 语音消息按角色和设备范围检索播放。
- LoRa 对讲必须使用专用实时语音硬件实测。

### 6.4 跌落撞击

- 使用 IMU 识别碰撞、跌落和晃动。
- 使用标注数据统计识别率、误报率和漏报率。
- 告警关联位置和媒体。
- 后台提供声音、画面、地图标记和处置审计。

### 6.5 近电

- 连续采集工频电场并自动建立基线。
- 保存强度、累积值、时间、位置和等级。
- 按已标定阈值分级。
- 断网时立即执行语音、LED 和振动，恢复后补传。

### 6.6 高度

- 连续采集并更新垂直高度。
- 使用可配置阈值和滞回评估风险。
- 超阈值时执行语音、LED 和振动。
- 事件持久化并上传后台。

## 7. 工程阶段

### 阶段 0：基线

核对需求、旧项目、板卡和工具链，建立架构、接口、需求矩阵、问题清单和测试计划。

### 阶段 1：Android 基础

完成多模块工程、前台服务、稳定设备 ID、Room、本地事件日志、专用离线队列、开机恢复和开发板安装。

### 阶段 2：用户态硬件接入

完成外部模块帧协议、AIDL/JNI 串口服务、模拟器、超时重试、心跳和本地告警接口。

### 阶段 3：媒体

完成相机、录像、元数据、完整性、存储配额、离线队列和后台归档。

### 阶段 4：定位

完成 GNSS/RTK 接入、轨迹、围栏、网络诊断、角色权限和地图综合页。

### 阶段 5：通信

完成呼叫信令、WebRTC、广播、语音消息和专用 LoRa 语音模块接口。

### 阶段 6：安全检测

完成 IMU、近电和高度算法、配置、回放、本地动作、可靠上传和后台处置。

### 阶段 7：开发板联调

使用真实模块完成安装、重启、按键、媒体、网络、定位、对讲、安全传感器和后台端到端验收。

## 8. 阶段门

每个阶段必须同时提供：

1. 可审查代码和配置。
2. 自动测试结果。
3. 指定 H618 开发板的运行或接口记录。
4. 有真实硬件时的原始测量或后台记录。
5. 需求矩阵、Goal 状态和待确认问题更新。
6. 已知限制和下一入口。

模拟器只证明软件行为。模拟位置、固定电量、合成媒体和合成传感器数据不能替代最终验收。

## 9. 外设接入流程

1. 取得模块型号、数据手册、接口、电平和接线。
2. 在 Android 上确认枚举、设备节点或厂商 API。
3. 记录权限和可重复初始化步骤。
4. 使用协议测试验证正常帧、损坏帧、超时、重试和重连。
5. 将数据接入业务模型、Room 和后台。
6. 执行断网、重启和模块断开测试。
7. 将真实证据保存到 `docs/verification/`。

若需要修改系统软件才能启用接口，停止当前接入并记录外部依赖。

## 10. 后台联调

`deploy/backend-stack/` 提供 PostgreSQL、MQTT TLS、后台和 Caddy。外部 S3、TURN、地图和证书通过配置注入。

联调至少验证：

- 设备身份和组织隔离。
- MQTT 主题和证书设备 ID 一致。
- 轨迹、媒体、告警和通信幂等。
- 对象正文大小和 SHA-256。
- 断网恢复和重复消息。
- 管理端角色权限和拒绝审计。

## 11. 测试与证据

执行：

```sh
./tools/test-android.sh
./tools/test-backend.sh
python3 tools/validate-requirements-traceability.py
```

实机记录必须包含硬件型号、接线、配置、环境、步骤、原始结果、APK 摘要和结论。证据路径写入 `docs/REQUIREMENTS_TRACEABILITY.md`。

## 12. 完成条件

只有以下条件全部满足，Goal 才能完成：

- 31 个 REQ 全部为已完成。
- Android、用户态硬件接入、本地告警、后台和管理端形成真实闭环。
- 固定和模拟数据已替换为真实数据。
- 真实硬件、弱网、离线和重启恢复测试全部通过。
- 开发板软件和后台可按文档重复部署。
- 协议、构建、测试和证据完整归档。

硬件或外部服务缺失时继续完成不受阻塞的工作，并保持 Goal 进行中。
