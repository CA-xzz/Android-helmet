# 模拟与数据回放

`datasets/stage-6/` 保存阶段 6 的可回放传感器数据。CSV 必须包含数据集 ID、采样率、来源和 `final_hardware_evidence` 元数据。合成数据固定标记为 `false`，只用于算法回归，不属于真实硬件验收证据。

当前数据集分别覆盖自由落体后撞击、独立碰撞、剧烈晃动、近电基线后电场上升和高度阈值变化。Android `safety-detection` 模块使用生产算法完成严格解析、顺序校验、确定性回放和误报漏报统计。
