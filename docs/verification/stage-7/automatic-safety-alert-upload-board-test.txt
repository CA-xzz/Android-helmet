H618 前台服务自动安全告警上传验证

日期：2026-08-11（Asia/Shanghai）
设备：QUAD-CORE H618 p1
ADB 序列号：2c001031774186e21d3
Android：12，API 31
应用版本：0.2.0，versionCode 2
APK SHA-256：3c5dd923951760b564cdd48fad69560a09fcdbe23917d85dd209d69330b519ba

目的

验证 H618 前台服务接收模拟跌倒输入后，自动完成原始样本持久化、告警持久化、SafetyAlertWorker 调度、后台上传和告警查询。测试不直接写入样本或告警，不直接创建、入队或运行 SafetyAlertWorker，也不直接调用告警上传接口。

环境

- Android 状态为 Active default network: none。
- 本地隔离后台监听 http://127.0.0.1:18083，通过 adb reverse tcp:18083 tcp:18083 接入。
- 精确回环地址不要求 WorkManager 网络约束；远端后台地址仍要求已连接网络。
- H618 后台 instrumentation 进程不能直接启动应用服务。测试使用 UiAutomation shell 权限发送显式 ACTION_SIMULATE 服务 Intent，后续处理全部由应用前台服务执行。
- 输入为 SimulatedInput.FALL，不是 IMU 实测数据。

命令

adb -s 2c001031774186e21d3 install device-android/app/build/outputs/apk/debug/app-debug.apk
adb -s 2c001031774186e21d3 install -t device-android/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
adb -s 2c001031774186e21d3 reverse tcp:18083 tcp:18083
adb -s 2c001031774186e21d3 shell am instrument -w -r -e class com.example.helmet.AutomaticSafetyAlertUploadInstrumentedTest com.example.helmet.test/androidx.test.runner.AndroidJUnitRunner

测试结果

- AutomaticSafetyAlertUploadInstrumentedTest：1 项通过，0 失败，用时 8.822 秒。
- 前台服务保持 isForeground=true，foregroundId=1001。
- 服务收到 FALL 后进入 SOS，状态记录 pendingSafetyAlertCount=1、retainedSafetySampleCount=1、lastEventType=SIMULATED_FALL_ACTIVE。
- SafetyAlertWorker 在事件后自动运行并返回 SUCCESS。测试代码没有调用 Worker 或 enqueue 方法。

设备数据库

- deviceId：6309dc08-d3c5-4a80-bb0e-e80d2a6d9af8。
- sampleReference：18563491；monotonicMillis：18564985；validFlags：1。
- accelerationXMilliG：2400；simulated：true。
- alertId：6309dc08-d3c5-4a80-bb0e-e80d2a6d9af8:18563491。
- 告警字段：type=FALL、severity=HIGH、active=true、configVersion=1、locationFixType=NO_FIX。
- 本地交付状态：DELIVERED；attemptCount=1；lastError 为空。
- 事件记录包含 SIMULATED_FALL_ACTIVE 和 SAFETY_ALERT_UPLOAD_COMPLETED，后台返回 deduplicated=false。

后台结果

- workflowState=OPEN，workflowSequence=1，requiresAttention=true。
- presentation.sound=true，presentation.visualPriority=HIGH。
- presentation.mapMarker=false，因为本轮没有可信位置。
- sensorSnapshot 保留 sampleReference、三轴加速度、三轴陀螺仪、validFlags、detectionOrigin=SIMULATOR 和 simulated=true。
- 告警事件和设备记录使用相同 alertId、messageId 和 sampleReference。

软件回归

- Android 单元测试和 Lint：通过。
- Android APK 构建：通过。
- 后台测试：70 项通过。
- 需求追踪校验：31 项通过。
- 工具测试：10 项通过。

结论

H618 在没有 Android 默认网络的 ADB 回环条件下，能由前台服务自动完成模拟跌倒样本、告警持久化和 SafetyAlertWorker 上传，后台查询结果与设备记录一致。该结果证明自动调度和端到端软件路径，不替代真实 IMU 动作识别率、真实位置、摄像头媒体证据、移动网络或现场告警验收。REQ-5.4-02 保持软件完成待实机。
